# DataIQ — Python Dependencies

# Core data stack
pandas>=2.0.0
numpy>=1.24.0
openpyxl>=3.1.0        # Excel .xlsx read/write
xlrd>=2.0.1            # Legacy .xls support
tabulate>=0.9.0        # DataFrame .to_markdown()

# Web server
flask>=3.0.0
flask-cors>=4.0.0

# Optional LLM backends (uncomment what you use)
# openai>=1.30.0        # OpenAI GPT-4o / GPT-4o-mini
# requests>=2.31.0      # Ollama local LLM

# Optional charting (for chart generation in code snippets)
# matplotlib>=3.8.0
# plotly>=5.20.0
